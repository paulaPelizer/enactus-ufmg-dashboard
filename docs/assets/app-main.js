async function login(e){e.preventDefault();const user=$('#username').value.trim(),pass=$('#password').value,err=$('#login-error');err.textContent='';const btn=$('#login-form .primary-button');btn.disabled=true;btn.textContent='Entrando…';try{if(!user||!pass)throw new Error('Preencha usuário e senha.');await loadDashboardData();state.sessionUser=user;showApp()}catch(ex){err.textContent=ex.message||'Não foi possível entrar.'}finally{btn.disabled=false;btn.textContent='Entrar no painel'}}
async function refreshRadar(){const b=$('#refresh-radar'),old=b.textContent;b.textContent='Atualizando…';b.disabled=true;try{state.radar=await loadJSON('data/radar.json?t='+Date.now());if(state.activeTab===state.dashboard.tabs.length)renderRadar()}catch(e){alert('Não foi possível recarregar o Radar agora.')}finally{b.textContent=old;b.disabled=false}}
document.addEventListener('DOMContentLoaded',()=>{$('#login-form').addEventListener('submit',login);$('#toggle-password').addEventListener('click',()=>{const p=$('#password');p.type=p.type==='password'?'text':'password'});$('#logout-button').addEventListener('click',()=>location.reload());$('#refresh-radar').addEventListener('click',refreshRadar)})